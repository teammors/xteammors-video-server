<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xteam.video.mapper.VideoTaskMapper">

    <select id="selectPendingTasks" resultType="com.xteam.video.entity.VideoTask">
        SELECT * FROM video_task
        WHERE status = 'pending'
        ORDER BY created_at ASC
        LIMIT 100
    </select>

    <select id="selectByUserId" resultType="com.xteam.video.entity.VideoTask">
        SELECT * FROM video_task
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByIdWithLock" resultType="com.xteam.video.entity.VideoTask">
        SELECT * FROM video_task
        WHERE id = #{id}
        FOR UPDATE SKIP LOCKED
    </select>

</mapper>